/**
 * Client configuration for Fius SDK
 */
export interface ClientConfig {
    /** Base URL of the Fius server */
    baseUrl: string;
    /** Optional API key for authentication */
    apiKey?: string;
}
